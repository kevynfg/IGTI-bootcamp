function soma(a, b) {
  return `A soma de ${a} + ${b} é: ${a + b} \n`;
}

function subtracao(a, b) {
  return `A subtração de ${a} - ${b} é: ${a - b} \n`;
}

function olaPessoa(string) {
  return `olá, ${string} \n`;
}

export default { soma, subtracao, olaPessoa };
